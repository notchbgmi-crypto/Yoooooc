services:
  - type: web
    name: upload-bot
    runtime: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: python upload_bot.py
    disk:
      name: upload-bot-data
      mountPath: /var/data
      sizeGB: 1
